{
  "name": "nexify",
  "version": "1.0.0",
  "description": "A social networking platform with automated content moderation and context-based authentication system.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd server && npm ci && cd ../client && npm ci && npm run build",
    "build:all": "npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm run production",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm start",
    "install": "npm install --ignore-scripts && cd server && npm install && cd ../client && npm install"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^7.0.5"
  }
}
